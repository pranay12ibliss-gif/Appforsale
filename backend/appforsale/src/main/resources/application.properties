


spring.application.name=appforsale

server.port=8085

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/appsmarket_auth
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ─── Gmail SMTP ───────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=pranay12ibliss@gmail.com
spring.mail.password=uecp rysu mljl jzcw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ─── Contact Form Receiver ────────────────────────────────────
app.contact.receiver-email=pranay12ibliss@gmail.com

# ─── Server Base URL (anni devices ki images reach avvadam ki) ───
# ✅ Mee PC WiFi IP — change cheyyakandi
app.server.base-url=http://192.168.1.8:8085

# ─── Static files — /uploads/ folder serve cheyyali ─────────
# ✅ Comment ledu — uncommented undi
spring.web.resources.static-locations=classpath:/static/,file:./uploads/
spring.mvc.static-path-pattern=/uploads/**

# ─── Upload size limits ───────────────────────────────────────
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB


# spring.application.name=appforsale

# server.port=8085

# # MySQL
# spring.datasource.url=${DB_URL}
# spring.datasource.username=${DB_USERNAME}
# spring.datasource.password=${DB_PASSWORD}
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# # JPA
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # Gmail SMTP
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${MAIL_USERNAME}
# spring.mail.password=${MAIL_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
# spring.mail.properties.mail.smtp.starttls.required=true
# spring.mail.properties.mail.smtp.connectiontimeout=5000
# spring.mail.properties.mail.smtp.timeout=5000
# spring.mail.properties.mail.smtp.writetimeout=5000

# # Contact Form Receiver
# app.contact.receiver-email=${CONTACT_EMAIL}

# # Server Base URL
# app.server.base-url=${SERVER_BASE_URL:http://192.168.1.3:8085}

# # Static files
# spring.web.resources.static-locations=classpath:/static/,file:./uploads/
# spring.mvc.static-path-pattern=/uploads/**

# # Upload size limits
# spring.servlet.multipart.max-file-size=10MB
# spring.servlet.multipart.max-request-size=50MB
